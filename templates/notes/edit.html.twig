{% extends 'base.html.twig' %} {% block title %}Modifier note de
{{ note.eleveId.nom }} {{ note.eleveId.prenom }}{% endblock %} {% block body %}

    <div class="wrapper">
        <div class="sidebar">
          <div class="sidebar-menu">
            <a href="{{ path("app_main") }}" data-section="section1">Acceuil</a>
            <a href="{{ path("app_notes_index") }}" data-section="section1" class="active">Liste des notes</a>
            <a href="{{ path("app_eleves_choice") }}" data-section="section2">Saisir une note</a>
            <a href="{{ path("app_notes_moyennes") }}" data-section="section2">Voir les moyennes</a>
          </div>
        </div>
        <div class="center">
        <h1>Modifier la note de {{ note.eleveId.nom }} {{ note.eleveId.prenom }}</h1>
        
        {{ form_start(form) }}
          {{ form_row(form.note) }}
          {{ form_row(form.type_evaluation) }}
          {{ form_row(form.date_evaluation) }}
          {{ form_row(form.matiere_id) }}
          <div class="hidden">{{ form_row(form.eleve_id) }}</div>
          <button class="btn btn-primaire my-4">
            {{ button_label|default('Enregistrer') }}
          </button>
        {{ form_end(form) }}
        
        {{ include("notes/_delete_form.html.twig") }}
        </div>
    </div>
{% endblock %}
