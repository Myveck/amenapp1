{% extends 'base.html.twig' %} {% block title %}Modifier note de
{{ note.eleveId.nom }} {{ note.eleveId.prenom }}{% endblock %} {% block body %}

    <div class="wrapper">
        <div class="sidebar">
        <div class="sidebar-menu">
            <a href="{{ path("app_main") }}" data-section="section1">Acceuil</a>
            <a href="{{ path("app_notes_index") }}" data-section="section1">Liste des notes</a>
            <a href="{{ path("app_eleves_choice") }}" data-section="section2" class="active">Saisir une note</a>
            <a href="{{ path("app_notes_moyennes") }}" data-section="section2">Voir les moyennes</a>
            <a href="{{ path("app_notes_bulletins") }}" data-section="section2"  >Voir les bulletins</a>
        </div>
        </div>
        <div class="center">
        <h1>Créer une note pour <strong>{{ note.eleveId.nom }} {{ note.eleveId.prenom }}</strong></h1>
        
        {{ form_start(form) }}
            {{ form_row(form.evaluation) }}
            {{ form_row(form.matiere_id) }}
            {{ form_row(form.note) }}
            {{ form_row(form.date_evaluation) }}
            {{ form_row(form.trimestre) }}
            <div class="hidden">
                {{ form_row(form.eleve_id) }}
            </div>
            <button class="btn btn-primaire my-4">Enregistrer</button>
        {{ form_end(form) }}
        
        </div>
    </div>
{% endblock %}
