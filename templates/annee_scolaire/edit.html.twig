{% extends 'base.html.twig' %}

{% block title %}Edit AnneeScolaire{% endblock %}

{% block body %}
    <div class="wrapper">
        <div class="sidebar">
            <div class="top"><a href="{{ path("app_main") }}" data-section="section1">Acceuil</a></div>
            <div class="sidebar-menu">
                <a href="{{ path("app_ecoles_edit", {'id': 1 }) }}" data-section="section1" >Ecole</a>
                <a href="{{ path("app_annee_scolaire_edit", {'id': 1}) }}" data-section="section1" class="active">Années scolaires</a>
            </div>
        </div>
        <div class="center">
            <h1>Année en cours</h1>
            {{ include('annee_scolaire/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>
    </div>
{% endblock %}
