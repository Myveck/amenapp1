{% extends 'base.html.twig' %}

{% block title %}Edit Paiements{% endblock %}

{% block body %}
    <div class="wrapper">
        <div class="sidebar">
            <div class="sidebar-menu">
                <a href="{{ path("app_main") }}" data-section="section1">Acceuil</a>
                <a href="{{ path("app_main_finances") }}" data-section="section1">Dashboard</a>
                <a href="{{ path("app_paiements_index") }}" data-section="section1"  class="active">Paiements</a>
                <a href="{{ path("app_paiements_etat") }}" data-section="section2">Etat</a>
            </div>
        </div>
        <div class="center">
            <h1>Modifier le paiement de <strong>{{paiement.eleveId.nom}} {{paiement.eleveId.prenom}}</strong></h1>
            {{ include('paiements/_form.html.twig', {'button_label': 'Modifier'}) }}
            <a href="{{ path('app_paiements_index') }}"><-</a>
            {{ include('paiements/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
