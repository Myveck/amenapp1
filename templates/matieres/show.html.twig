{% extends 'base.html.twig' %}

{% block title %}Matieres{% endblock %}

{% block body %}
    <div class="wrapper">
        <div class="sidebar">
            <div class="sidebar-menu">
            <a href="{{ path("app_main") }}" data-section="section1">Acceuil</a>
            <a href="{{ path("app_matieres_index") }}" data-section="section1">Liste des matières</a>
            <a href="{{ path("app_matieres_new") }}" data-section="section2"class="active">Ajouter une matières</a>
            </div>
            </div>
        <div class="center">
            <h1>{{ matiere.nom }}</h1>
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ matiere.id }}</td>
                    </tr>
                    <tr>
                        <th>Nom</th>
                        <td>{{ matiere.nom }}</td>
                    </tr>
                </tbody>
            </table>
            <div class="small-wrapper">
                <p><a href="{{ path('app_matieres_index') }}" class="btn btn-primaire">Retours vers la liste</a></p>
                <p><a href="{{ path('app_matieres_edit', {'id': matiere.id}) }}" class="btn btn-secondaire">Modifier</a></p>
            </div>
            {{ include('matieres/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
