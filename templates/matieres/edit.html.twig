{% extends 'base.html.twig' %}

{% block title %}Edit Matieres{% endblock %}

{% block body %}
    <div class="wrapper">
        <div class="sidebar">
            <div class="sidebar-menu">
            <a href="{{ path("app_main") }}" data-section="section1">Acceuil</a>
            <a href="{{ path("app_matieres_index") }}" data-section="section1">Liste des matières</a>
            <a href="{{ path("app_matieres_new") }}" data-section="section2"class="active">Ajouter une matières</a>
            </div>
        </div>
        <div class="center">
            <h1>Modifier <strong>{{ matiere.nom }}</strong></h1>
            {{ include('matieres/_form.html.twig', {'button_label': 'Enregistrer'}) }}
            {{ include('matieres/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
