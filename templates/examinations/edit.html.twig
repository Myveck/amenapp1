{% extends 'base.html.twig' %}

{% block title %}Edit Examinations{% endblock %}

{% block body %}
    <h1>Edit Examinations</h1>

    {{ include('examinations/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_examinations_index') }}">back to list</a>

    {{ include('examinations/_delete_form.html.twig') }}
{% endblock %}
