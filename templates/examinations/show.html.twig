{% extends 'base.html.twig' %}

{% block title %}Examinations{% endblock %}

{% block body %}
    <h1>Examinations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ examination.id }}</td>
            </tr>
            <tr>
                <th>Date_examination</th>
                <td>{{ examination.dateExamination ? examination.dateExamination|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_examinations_index') }}">back to list</a>

    <a href="{{ path('app_examinations_edit', {'id': examination.id}) }}">edit</a>

    {{ include('examinations/_delete_form.html.twig') }}
{% endblock %}
